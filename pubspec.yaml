name: confectionery_storie
description: My Confectionery Storie
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  flutter_modular: ^3.3.1
  flutter_triple: ^1.2.3+1
  rx_notifier: ^1.1.0
  google_fonts: ^2.1.0
  font_awesome_flutter:
  auto_size_text:
  flutter_secure_storage: ^4.2.0
  hive_flutter: ^1.1.0
  hive: ^2.0.4
  path_provider: ^2.0.2
  flutter_mobx: ^2.0.1
  mobx: ^2.0.3
dev_dependencies:
  mobx_codegen: ^2.0.2
  build_runner: ^2.0.6
  hive_generator: ^1.1.0

  flutter_test:
    sdk: flutter
  flutter_modular_test: ^1.0.1

flutter:
  uses-material-design: true